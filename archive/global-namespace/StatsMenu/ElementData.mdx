---
name: ElementData
type: type
typekind: class
enclosingType: StatsMenu
namespace: .global

sidebar_label: ElementData
description: A nested private class used internally by StatsMenu to store data about individual UI elements.
---

# [StatsMenu](/api/global-namespace/StatsMenu/).ElementData Class

## Definition

**Namespace**: `<Global Namespace>`  
**Assembly**: `SFS.dll`  
**Version**: Added in SFS

A nested private class used internally by `StatsMenu` to store data about individual UI elements.

```csharp
private class StatsMenu.ElementData
```

**Inheritance**: [Object](https://learn.microsoft.com/en-us/dotnet/api/system.object?view=netframework-4.8) → StatsMenu.ElementData

## Members

### Fields

| Type     | Name          | Description                                                               |
| -------- | ------------- | ------------------------------------------------------------------------- |
| `int`    | `priority`    | An integer representing the priority of the element for sorting purposes. |
| `int`    | `index`       | TBD                                                                       |
| `Action` | `draw`        | An `Action` delegate that contains the method to draw the UI element.     |
| `bool`   | `statElement` | TBD                                                                       |
